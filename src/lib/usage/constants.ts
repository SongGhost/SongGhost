/** Free-tier monthly DJ break allowance (rolling 30-day window). */
export const FREE_MONTHLY_BREAK_LIMIT = 30;

/** Length of the metering window in milliseconds. */
export const USAGE_PERIOD_MS = 30 * 24 * 60 * 60 * 1000;
