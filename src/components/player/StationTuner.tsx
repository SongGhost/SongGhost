/**
 * Compatibility re-export — advanced tuning UI lives in the studio panel.
 */
export {
  default,
  type StationTunerResult,
  type TunerDecade,
  type TunerGenreOption,
} from "@/components/studio/TuneStationPanel";
